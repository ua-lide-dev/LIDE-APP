Quand on lance le script des docker on pourrait rajouter les dockers des compilations
docker run -i -d --name containercpp -v /root/data:/data cpp
lance le terminal en mode interactif + -d pour ne pas qu'il se mette en mode exit tout seul

ensuite on va mettre le fichier de l'etudiant 
(fichier.cpp par exemple) dans le dossier data (lide-back/images/data)
et lancer la commande 
	docker exec containercpp ./buildNrun.sh fichier.cpp

tous les containers se partage le volume /data 
 pb de ca si deux etudiants lance une compile avec le mm nom genre test.cpp ca va rm un des deux
ducoup on peut rename les fichiers avec genre tokenEtudiant_nomfichier.extension

mais ducoup tout les containers vont se partager /data et ducoup on a pas besoin de rebuild l'image !
parce que si on bouge /data il est bouge dans tous les containers aussi !
